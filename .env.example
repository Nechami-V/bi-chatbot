## BI Chatbot .env Example
# Copy this file to `.env` and adjust values

# --- Core API ---
API_HOST=0.0.0.0
API_PORT=8002
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8002

# --- Security / Auth ---
# Used by app.simple_config.Config
SECRET_KEY=change-this-to-a-secure-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=480

# --- Databases ---
# Main BI DB (SQLite example)
DATABASE_URL=sqlite:///./bi_chatbot.db

# External Users DB (SQL Server via ODBC)
# Windows Auth example:
# USER_DATABASE_URL=mssql+pyodbc://@localhost\\SQLEXPRESS/CHATBOTBI?driver=ODBC+Driver+17+for+SQL+Server&trusted_connection=yes&TrustServerCertificate=yes
# SQL login example:
# USER_DATABASE_URL=mssql+pyodbc://sa:YourPassword@localhost/CHATBOTBI?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes

# --- OpenAI ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_RETRIES=3
OPENAI_TIMEOUT=30
